export enum StatusEnum {
    evaluation = 'evaluation',
    shortlisted = 'shortlisted',
    won = 'won',
    lost = 'lost',
    pending = 'pending',
    cancelled = 'cancelled',
    // retender = 'retender',
    dropped = 'dropped',

}
